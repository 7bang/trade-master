<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Supabase uses HTTPS, but this ensures proper configuration -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Allow specific domains if needed (currently all must use HTTPS) -->
    <domain-config cleartextTrafficPermitted="false">
        <domain includeSubdomains="true">supabase.co</domain>
        <domain includeSubdomains="true">eloztkamiaemnscndlqb.supabase.co</domain>
    </domain-config>
</network-security-config>
